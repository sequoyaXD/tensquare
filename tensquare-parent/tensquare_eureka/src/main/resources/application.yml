server:
  port: 6868
eureka:
  client:
    register-with-eureka: false # 是否把本身微服务注册到Eureka
    fetch-registry:  false # 是否从Eureka获取注册信息
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka     # 指定Eureka注册地址